{% extends 'admin/base.html.twig' %}

{% block title "Nouvelle Catégorie" %}

{% block body_title "Ajouter une catégorie" %}

{% block body %}
    <div class="max-w-2xl mx-auto">
        <div class="dark-card shadow rounded-lg border dark-border overflow-hidden">
            <div class="px-4 py-5 sm:px-6 border-b dark-border flex justify-between items-center">
                <h3 class="text-lg font-medium text-text-primary">Informations de la catégorie</h3>
                <a href="/admin/categories" class="text-text-secondary hover:text-text-primary">
                    <i class="fas fa-arrow-left mr-1"></i> Retour
                </a>
            </div>
            
            <form action="/admin/tags/store" method="POST" class="px-4 py-5 sm:p-6 space-y-6">
                <div>
                    <label for="name" class="block text-sm font-medium text-text-secondary mb-1">Nom de le Tag</label>
                    <input type="text" name="name" id="name" required
                           class="dark-input block w-full rounded-md border-green-primary border-2 shadow-sm px-4 py-2 text-text-primary focus:ring-green-primary focus:border-green-primary"
                           placeholder="Ex: Laravel , React , Figma ...">
                    <p class="mt-1 text-xs text-text-secondary">Ce nom sera affiché publiquement.</p>
                </div>

                <div class="flex justify-end pt-4 border-t dark-border">
                    <button type="submit" class="dark-button inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-black shadow-sm">
                        <i class="fas fa-save mr-2"></i>
                        Enregistrer
                    </button>
                </div>
            </form>
        </div>
    </div>
{% endblock %}
