{% extends 'recruteur/base.html.twig' %}
{% block title "Candidatures - " ~ offer.name %}

{% block body_title "Candidatures pour : " ~ offer.name %}

{% block body %}
    <div class="mb-4">
        <a href="/recruteur/offres" class="text-sm dark-text-accent hover:underline">
            <i class="fas fa-arrow-left mr-1"></i>
            Retour aux offres
        </a>
    </div>

    <div class="dark-card shadow rounded-lg border dark-border">
        <div class="overflow-hidden">
            <table class="min-w-full divide-y divide-dark-border">
                <thead class="bg-dark-bg">
                <tr>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-text-secondary uppercase tracking-wider">
                        Candidat
                    </th>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-text-secondary uppercase tracking-wider">
                        Statut
                    </th>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-text-secondary uppercase tracking-wider">
                        Date
                    </th>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-text-secondary uppercase tracking-wider">
                        Message
                    </th>
                    <th scope="col" class="relative px-6 py-3">
                        <span class="sr-only">Actions</span>
                    </th>
                </tr>
                </thead>
                <tbody class="divide-y divide-dark-border">
                {% for candidature in candidatures %}
                    <tr>
                        <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-text-primary">
                            <div class="flex items-center">
                                <div class="h-8 w-8 rounded-full bg-dark-border flex items-center justify-center mr-3">
                                    <i class="fas fa-user text-text-secondary"></i>
                                </div>
                                <div>
                                    {{ candidature.user.name }}
                                    <div class="text-xs text-text-secondary">{{ candidature.user.email }}</div>
                                </div>
                            </div>
                        </td>
                        <td class="px-6 py-4 whitespace-nowrap text-sm">
                            {% if candidature.status == 'pending' %}
                                <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-yellow-100 text-yellow-800">En attente</span>
                            {% elseif candidature.status == 'accepted' %}
                                <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-green-100 text-green-800">Acceptée</span>
                            {% elseif candidature.status == 'rejected' %}
                                <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-red-100 text-red-800">Refusée</span>
                            {% endif %}
                        </td>
                        <td class="px-6 py-4 whitespace-nowrap text-sm text-text-secondary">
                            {{ candidature.createdAt|date('d/m/Y') }}
                        </td>
                        <td class="px-6 py-4 text-sm text-text-secondary max-w-xs truncate">
                            {{ candidature.message|default('Pas de message') }}
                        </td>
                        <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
                            {% if candidature.status != 'accepted' %}
                                <a href="/recruteur/offres/candidatures/accept?id={{ candidature.id }}" class="text-green-500 hover:text-green-400 mr-3">Accepter</a>
                            {% endif %}

                            {% if candidature.status != 'rejected' %}
                                <a href="/recruteur/offres/candidatures/reject?id={{ candidature.id }}" class="text-red-500 hover:text-red-400 mr-3">Refuser</a>
                            {% endif %}
                            <a href="{{ candidature.cvUrl }}" target="_blank" class="dark-text-accent hover:text-green-primary">CV</a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="5" class="px-6 py-10 text-center text-sm text-text-secondary italic">
                            Aucune candidature pour le moment.
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
